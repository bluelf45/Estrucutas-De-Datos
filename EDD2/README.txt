Se utilizo WSL para compilar el programa.
Solo se requiere compilar usando "gcc TDAlista.c ejercicio1.c -o main -Wall" y correr el programa con el archivo input1.dat en la misma carpeta que el resto del programa (Pregunta 1)
Se debe usar "gcc TDArbol.c main.c -o main -Wall" y se debe tener el archivo "input.txt" en la misma carpeta que el resto del programa(ejercicio 2)